FROM eclipse-temurin:17-jdk-jammy

# 1. 필수 패키지 설치 (Python3, PIP, FFMPEG, CURL)
RUN apt-get update && apt-get install -y \
    python3 \
    python3-pip \
    ffmpeg \
    curl \
    && rm -rf /var/lib/apt/lists/*

# 2. yt-dlp 설치 (GitHub에서 최신 릴리스 정보를 확인하여 캐시 무효화)
ADD https://github.com/yt-dlp/yt-dlp/releases/latest /tmp/yt-dlp-latest
RUN curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp
RUN chmod a+rx /usr/local/bin/yt-dlp && rm /tmp/yt-dlp-latest

# 3. 작업 디렉토리 설정
WORKDIR /app
